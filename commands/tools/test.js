const Discord = require('discord.js')

module.exports = {
    name: 'test',
    description: 'just test.',
    aliases: ['t'],
    dm : false,

    async run(bot, message, args, footer) {
        
    }
};